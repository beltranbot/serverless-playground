module.exports = {
  target: 'node', // what are we trying to compile?
  // mode: 'production' // production compress all the code into a single file
  mode: 'none' // only the code that is needed is deployed
}
